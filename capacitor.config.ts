import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harshwebsitebuilder.app',
  appName: 'Harsh Website Builder',
  webDir: 'dist'
};

export default config;
